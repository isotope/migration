{% extends "base.twig" %}

{% block main %}
    <div class="row headline">
        <h2>{% trans %}base.summary{% endtrans %}</h2>
    </div>
    <div class="row">
        {% for service in messages %}
            <h3>{{ service.title }}</h3>
            <ul>
                {% for message in service.messages %}
                    <li>{{ message }}</li>
                {% endfor %}
            </ul>
        {% endfor %}
    </div>
{% endblock %}
